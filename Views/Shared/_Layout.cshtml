<!DOCTYPE html>
<html lang="fr">
<head>
<<<<<<< HEAD
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>@ViewData["Title"] — VoteFokontany</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true"/>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" asp-controller="Dashboard" asp-action="Index">
            Vote<span style="color:#E74C3C">Fokontany</span>
        </a>
        @if (Context.Session.GetString("user") != null)
        {
            <div class="navbar-nav me-auto">
                <a class="nav-link" asp-controller="Dashboard" asp-action="Index">📊 Dashboard</a>
                <a class="nav-link" asp-controller="Citoyen" asp-action="Index">👥 Citoyens</a>
                <a class="nav-link" asp-controller="Election" asp-action="Index">📋 Élections</a>
                <a class="nav-link" asp-controller="Vote" asp-action="Index">🗳️ Voter</a>
            </div>
            <div class="navbar-nav">
                <span class="nav-link text-white-50">👤 @Context.Session.GetString("user")</span>
                <a class="nav-link" asp-controller="Account" asp-action="Logout">Déconnexion</a>
            </div>
        }
        else
        {
            <div class="navbar-nav ms-auto">
                <a class="nav-link" asp-controller="Account" asp-action="Login">Connexion</a>
                <a class="nav-link" asp-controller="Account" asp-action="Register">Inscription</a>
            </div>
        }
    </div>
</nav>
<main class="container-fluid py-4">
    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }
    @RenderBody()
</main>
<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
@await RenderSectionAsync("Scripts", required: false)
=======
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - E-VOTE</title>
    
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
        :root {
            --primary: #6366f1;
            --dark-bg: #0f172a;
            --nav-bg: rgba(15, 23, 42, 0.9);
        }

        body {
            font-family: "Inter", sans-serif;
            margin: 0;
            padding: 0;
            overflow-x: hidden; /* Sécurité anti-scroll horizontal */
            width: 100%;
        }

        /* Navbar Style Portfolio */
        .navbar-custom {
            background-color: var(--nav-bg) !important;
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            padding: 15px 0;
        }

        .navbar-brand-custom {
            font-weight: 700;
            font-size: 1.5rem;
            color: white !important;
        }

        .navbar-brand-custom span { color: var(--primary); }

        .nav-link-custom {
            color: rgba(255, 255, 255, 0.8) !important;
            font-weight: 500;
            margin-left: 20px;
            transition: 0.3s;
        }

        .nav-link-custom:hover { color: var(--primary) !important; }

        .btn-nav {
            background: var(--primary);
            color: white !important;
            border-radius: 8px;
            padding: 8px 20px;
            font-weight: 600;
            border: none;
            margin-left: 20px;
            transition: 0.3s;
        }

        .btn-nav:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.4);
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark navbar-custom fixed-top">
            <div class="container">
                <a class="navbar-brand navbar-brand-custom" asp-controller="Home" asp-action="Index">
                    VOTE<span>.</span>MG
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto align-items-center">
                        <li class="nav-item"><a class="nav-link nav-link-custom" href="/">Accueil</a></li>
                        <li class="nav-item"><a class="nav-link nav-link-custom" href="/Candidats">Candidats</a></li>
                        <li class="nav-item"><a class="nav-link nav-link-custom" href="/Account/Login">Connexion</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main role="main">
        @RenderBody()
    </main>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    @await RenderSectionAsync("Scripts", required: false)
>>>>>>> a228513f9f32357ac2e45ef74c82a2d6c0c185de
</body>
</html>